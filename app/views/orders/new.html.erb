<% content_for :title, t('orders.new.title') %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t('orders.new.title') %></h1>

  <%= render "form", order: @order %>

  <%= link_to t('orders.form.cancel_button'), orders_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium border border-gray-300" %>
</div>
